#include <stdio.h>

int main() {
   /* int n , count=0;
    printf("Enter a number=");
    scanf("%d",&n);
    for (int i = 1; i <=10; i++)
    {
        if (n%i==0)
        {
            count++;
        }
    }
        if (count==2) //divisor concept//
        {
            printf(" is prime no:");
        }else{
            printf("is not prime no;");
        } */
        

        /*int n , flag=1;
        printf("enter a number=");
        scanf("%d",&n);
        for (int i = 2; i < n; i++)
        {
            if (n%i==0)
            {
                flag=0;
                break;
            }
        }
            if (flag==1)
            {
               printf("this is prime.");
            }else{
               printf("this is not prime no.");
            }*/


            /*int n , flag = 1;
             printf("enter a number=");
             scanf("%d",&n);
             for (int i = 2; i <=n/2; i++)
             {
                if (n % i==0)
            {
                flag = 0;
                break;
            }
             }
            if (flag==1)
            {
               printf("this is prime.");
            }else{
               printf("this is not prime no.");
            }*/
            
             return 0;
}